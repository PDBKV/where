[build-system]
requires = ["meson-python", "ninja", "numpy>=1.26"]
build-backend = "mesonpy"

[project]
name = "where"
dynamic = ["version"]
description = "..."
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "You", email = "you@example.com"}]
classifiers = ["Programming Language :: Python :: 3"]
dependencies = [
    "numpy>=1.26",
]

[tool.setuptools.dynamic]
version = {attr = "where.__version__"}